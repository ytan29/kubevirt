---
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  labels:
    kubevirt.io/vm: vm-cirros-sata
  name: vm-cirros-sata
spec:
  running: false
  template:
    metadata:
      labels:
        kubevirt.io/vm: vm-cirros-sata
    spec:
      domain:
        devices: {}
        resources:
          requests:
            memory: 128Mi
      terminationGracePeriodSeconds: 0
      volumes:
      - containerDisk:
          image: localhost:5000/kubevirt/cirros-container-disk-demo:v0.57.1-dev
        name: containerdisk
      - cloudInitNoCloud:
          userData: |
            #!/bin/sh

            echo 'printed from cloud-init userdata'
        name: cloudinitdisk
